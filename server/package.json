{
  "name": "ozmedia-google-reviews-proxy",
  "version": "1.0.0",
  "description": "Simple proxy to fetch Google Place Details (reviews) server-side to keep API key private.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.1.0",
    "express": "^4.18.2"
  }
}
